#include "OneAxis.hpp"
#include <iostream>

namespace module {
namespace HardwareIO {
    namespace joint {

        OneAxis::OneAxis() {}

        // Some sort of initial position, 0
        // Some sort of initial length for all of the muscles

        // Input position

        double L_1 = 2 * l_01 + l_02 - L_2;
        double L_2 = (2 * M_PI() * r_1 * / 360);

    }  // namespace joint
}  // namespace HardwareIO
}  // namespace module